# 2.4.2 (2026-03-25)

- 新增：启用 API 查询与调用工具（`jlceda_api_index`、`jlceda_api_search`、`jlceda_context_get`、`jlceda_api_invoke`），用于专用功能工具无法覆盖的操作场景。
- 优化：工具优先级规则：原理图检查、器件选型、器件放置为高优先级专用工具，API 工具为托底工具，仅在专用工具均无法满足需求时才允许使用。
- 优化：新增 API 调用三步顺序强制规则：必须先用 `jlceda_api_index` 获取索引，再用 `jlceda_api_search` 确认签名，最后才能用 `jlceda_api_invoke` 执行调用，禁止跳过任意步骤。

# 2.4.1 (2026-03-24)

- 变更：器件放置流程不再自动放置电源和地符号，相关符号需要用户在原理图中手动添加。
- 变更：原理图检查工具返回结果从精简网表（`netlist`）改为器件布局图（`componentLayout`），包含每个器件的位号、坐标、旋转、镜像、封装，以及每个引脚的名称、编号、类型、坐标、旋转、引脚长度、连接状态，支持自动连线分析。

# 2.4.0 (2026-03-24)

- 新增：器件选型工具：AI 在原理图中放置器件时，会先搜索系统库中的候选器件，以列表形式展示，用户确认选择后再执行放置操作。
- 新增：器件放置工具：AI 选型完成后，可直接在原理图中执行器件放置，引导用户逐个点击确认放置位置。
- 优化：任务面板：执行过程折叠条目的标题现在显示当前正在进行的任务步骤名称，而不是固定的"执行过程"，刷新页面后标题仍能正确恢复。
- 优化：候选器件列表面板样式，滚动条改用自定义滚动库渲染，鼠标悬停时显示，离开后自动隐藏，与聊天主界面风格保持一致。

# 2.3.1 (2026-03-23)

- 修复：使用 Gemini 等模型时，同时调用多个工具（如同时建立任务列表并获取上下文）会出现执行失败的问题。

# 2.3.0 (2026-03-22)

- 新增：聊天页和模型配置页在加载期间，页面中心显示转圈动画，加载完成后自动消失，避免出现空白页面。【未生效，在此标记】
- 优化：聊天循环检测改为仅按 AI 返回文本判定，连续 3 次相同回复才自动停止，工具调用不再计入该限制。
- 新增：任务列表工具：任务会独立显示在输入框上方，支持标题点击折叠和展开，聊天正文更干净。
- 新增：原理图完整检查工具：可直接完成检查并返回可读结论，减少手动拆分检查步骤。

# 2.2.3 (2026-03-20)

- 修复：自定义平台 API 验证更准确，填写 Base URL 时不再误判为验证通过。
- 优化：原理图功能性审查速度更快，AI 能更高效地获取电路全貌。
- 优化：ERC/DRC 检查结果判断更准确，不再出现检查已通过但仍反复尝试获取详细结果的情况。

# 2.2.2 (2026-03-19)

- 新增：支持上传文档（.txt、.md、.csv 等纯文本格式）到聊天输入框，最多同时添加 10 个，AI 可直接读取文档内容。
- 移除：取消会话标题自动生成功能。
- 移除：取消思考模式开关。

# 2.2.1 (2026-03-18)

- 优化：AI 在原理图中放置器件时，会自动将器件布局在图纸中心区域，不再出现摆到边角或超出红框的情况。
- 新增：用户说检查原理图时，AI 默认进行功能性审查（分析电路用途、器件选型、引脚连接、能否正常工作），只有明确说跑 ERC 或做 DRC 时才执行规则检查。

# 2.2.0 (2026-03-18)

- 新增：AI 陷入循环（连续重复相同操作无法推进）时，自动终止并提示用户切换模型。
- 新增：支持 Anthropic 平台（Claude 系列模型原生接口），可在模型配置页添加。
- 变更：原自定义平台重命名为 OpenAI 自定义，与新增的 Anthropic 自定义平台区分。
- 移除：去掉百度文心一言预置平台，用户仍可通过自定义平台自行接入。
- 优化：多平台时模型配置选项卡显示更整洁，文字不再被挤压。

# 2.1.0 (2026-03-17)

- 修复：选择自定义平台时，图片上传按钮点击无响应的问题。

# 2.0.0 (2026-03-16)

- 扩展更名为：AI 设计助手。
- 新增：自定义平台支持，可填写任意 OpenAI 兼容接口地址，对接各类第三方大模型。
- 新增：AI 执行导出操作（BOM、网表等）后，回复中直接提供可点击的下载链接，点击即可下载文件。

# 1.1.0 (2026-03-08)

- 优化：界面视觉全面升级，深色/浅色主题下字体、颜色、大小均有改善。
- 新增：设置页面支持自定义指令。
- 新增：增加智谱大模型平台支持，以及思考模式独立开关。
- 新增：多会话本地持久化管理，会话记录保留在侧边栏列表中。
- 新增：会话标题由 AI 自动生成并显示在会话列表中。
- 新增：设置页增加 B 站视频使用教程入口，点击即可弹出视频页面。

# 1.0.1 (2026-02-28)

- 新增：模型配置页增加充值提醒，提示获取 API Key 后需先充值，避免因余额不足导致验证失败。
- 修复：三个平台的接口地址锁定为只读，防止误修改。

# 1.0.0 (2026-02-26)

初始版本

- 聊天面板：与 AI 对话，辅助 EDA 设计流程。
